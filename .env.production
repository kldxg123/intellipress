# 生产构建（GitHub Pages 静态版）的 LLM 代理端点。
# 部署 Cloudflare Worker（步骤见 worker/README.md）后，把其 workers.dev URL 填入下方等号后，例如：
#   VITE_LLM_PROXY_URL=https://intellipress-llm-proxy.<你的账户子域>.workers.dev
# 然后 git push 触发 Pages 重建即可。
# 留空时前端自动进入回放兜底模式（预置解释文案），静态演示不受影响。
# 注意：本文件入库，但绝不包含任何密钥；DeepSeek 密钥只存在 Workers secret 里。
VITE_LLM_PROXY_URL=https://intellipress-llm-proxy.1584800184.workers.dev
